/**
 * Legacy hook kept for App wiring. The notch window now derives visibility
 * inside NotchPanel because the hide rules depend on overlays, modes, and ESC silence.
 */
export function useAutoHide() {}
